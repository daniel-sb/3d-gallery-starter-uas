import { defineConfig } from "vite";

export default defineConfig({
  // For local dev keep "/". When deploying to a GitHub Pages project site,
  // set this to "/<nama-repo>/" (mis. "/3d-art-gallery/").
  base: "/",
});
